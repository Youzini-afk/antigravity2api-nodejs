services:
  antigravity2api:
    build: .
    env_file:
      - ./.env
    ports:
      - "${COMPOSE_HOST_PORT:-8046}:8046"
    volumes:
      - ./data:/app/data
      - ./public/images:/app/public/images
      - ./.env:/app/.env
      - ./config.json:/app/config.json
    restart: unless-stopped
